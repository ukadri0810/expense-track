const todayISO=new Date().toISOString().slice(0,10);const defaultState={salary:0,budget:{needs:50,wants:30,savings:20},recurring:[],expenses:[]};let state=JSON.parse(localStorage.getItem('spendwiseDailyV3'))||defaultState;const rupee=n=>'₹'+Math.round(Number(n||0)).toLocaleString('en-IN');function save(){localStorage.setItem('spendwiseDailyV3',JSON.stringify(state));location.reload()}function dailyLimit(){return state.recurring.reduce((s,i)=>s+i.amount,0)}function todaySpend(){return state.expenses.filter(e=>e.date===todayISO).reduce((s,e)=>s+e.amount,0)}function monthlyRecurring(){return state.recurring.reduce((s,i)=>s+i.amount*(i.frequency==='workdays'?22:30),0)}function catSpend(c){return state.expenses.filter(e=>e.category===c).reduce((s,e)=>s+e.amount,0)}function setText(id,v){let el=document.getElementById(id);if(el)el.innerHTML=v}function initCommon(){let d=document.getElementById('date');if(d)d.value=todayISO;setText('salaryText',rupee(state.salary));setText('dailyLimitText',rupee(dailyLimit()));setText('todaySpendText',rupee(todaySpend()));setText('monthlyHabitText',rupee(monthlyRecurring()))}function resetAll(){if(confirm('Delete all saved data from this phone?')){localStorage.removeItem('spendwiseDailyV3');location.href='index.html'}}
